# 2026-07-18 - mobai

## Session

- Device/server: `mobai`; repo: `/home/ubuntu/1Projects/deep-vein-gcn`; branch: `main` at `dd9397d`.
- Checked all 5,904 current FV-USM WLD MAT features: every template is constant zero, so the defect is global rather than limited to intra held-out identities.
- Evaluated the older full-FV-USM nonzero WLD score/rate artifacts in `tmp/wld/fvusm/`: AUC `83.07`, EER `22.87`, TAR@FAR `0.1/1/10%` = `0.00/0.08/20.58` under the paper's legacy metric convention.
- The full-score result matches the existing paper row, confirming that `tmp/wld/fvusm/` was produced from a valid earlier WLD export that does not correspond to the current all-zero `features/wld/fvusm/` tree.
- Drafted `rebuttal/response_letter_2.tex` for the second review round, covering FV-300 availability, the intra-database MCP/RLT comparison, and the DSConv morphology argument.
- Kept Experimental Sections 3--6 separate in the Reviewer 3 response because comparative evaluation, ablation, computational cost, and interpretation serve distinct purposes in the intended paper structure.
- Checked the new response letter against `rebuttal/response_letter.tex`: the preamble matches, all environments and delimiters are balanced, and the reviewer counters reset correctly.
- Softened the Reviewer 1 checkpoint response: acknowledged fully open distribution as a future consideration without claiming a change in this revision.

## Next

- Recover or regenerate the valid WLD templates before computing a held-out-only intra-dataset WLD row.
- Ensure the public FV-300 access URL appears in the submitted manuscript and repository README; the current local/public README snapshot only shows checkpoint access.
