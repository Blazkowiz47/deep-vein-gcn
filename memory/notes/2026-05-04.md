# 2026-05-04

## Session

- Device/server: raghu2 / `/home/ubuntu/sushrut/gcn-deep-vein`.
- Task: Update `rebuttal/paper.tex` half-subject and full-subject operating-point tables from `final_tables.md`.
- Changed `rebuttal/paper.tex`: updated the stale half-subject `ABDE -> C` LGFIN row, `ABCD -> E` zero-TAR bolding, and the regenerated full-subject rows/CI values for all affected splits.
- Also updated adjacent full-subject prose so the reported proposed-method EERs for `ABDE -> C` and `ABCE -> D` now read `7.45%` and `5.20%`, matching the table.
- Verification: read back the edited table region and inspected `git diff -- rebuttal/paper.tex`.
- Updated `rebuttal/supplementary_material.tex` intra-database protocol paragraph: replaced the stale `50%/30%/20%` train/validation/test claim with the implemented 80% model-development identities and 20% fully held-out identities, evaluated through within-identity and cross-identity pair scores.
- Updated the supplementary intra-database split section from subject-disjoint train/validation wording to subject-disjoint development/test wording. The section now distinguishes development identity train/test image folders from the held-out 20% open-set test identities and removes PolyU/VERA from the intra split tables.
- Removed the redundant per-identity train/validation table from `rebuttal/supplementary_material.tex` Section 5 and kept only `tab:intra_split_stats`, with headers/caption updated for development IDs, development train/test image counts, and held-out test IDs.
- Updated `rebuttal/response_letter.tex`: four predefined `stat_seed` runs per leave-one-dataset-out setting replace stale five-fold/five-run claims; half-subject protocol now describes exhaustive pairwise enrolled-vs-non-enrolled score distributions; intra-database protocol now describes 80% development identities and 20% held-out open-set identities.
- Updated only `rebuttal/response_letter.tex` for checkpoint reproducibility wording: kept public GitHub code/scripts/model-definition claim, but changed checkpoint availability to README-linked `final_runs.zip` with password/access request.
- Re-aligned project memory initialization from the knowledge base: refreshed the Sushrut `AGENTS.md` block to the current node-aware note convention, synced six `memory/commands/` specs to the current templates, and updated `memory/devices.md` to use stable node wording for `raghu2`. No project source, experiment, config, data, or output files were changed for this alignment.

## Next

- If `final_tables.md` changes again, rerun a focused table/prose consistency pass before Overleaf export.
- Check `response_letter.tex` for any remaining intra-database `50/30/20` wording before final submission.
