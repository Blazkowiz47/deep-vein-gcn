---
date: 2026-05-01
project: gcn-deep-vein
device: raghu2
server: SALT
repo_path: /home/ubuntu/sushrut/gcn-deep-vein
branch: main
sync_status: draft
tags: [phd, journal-paper, biometrics, gcn, vein-recognition]
---

# 2026-05-01

## Work Done

- Re-ran a targeted consistency review of updated `rebuttal/paper.tex` and `rebuttal/response_letter.tex` with sub-agents for paper, response-letter, and code/artifact verification.
- Updated `rebuttal/inconsistencies.md` with a current-state section so resolved historical findings are separated from active issues.
- Ran another fresh three-agent review after the user updated `paper.tex` repetition wording. This pass ignored CAH formula simplification, Overleaf figure paths/build risks, and temporary checkpoint release links.
- Replaced `rebuttal/inconsistencies.md` with a concise current-only remaining issues checklist, removing historical/resolved findings from the file.
- Added a `Supplementary Material` section to `rebuttal/inconsistencies.md` and validated it with two sub-agents using the current checklist as context.

## Analysis Results

- The half-subject protocol in `paper.tex` now matches `scripts/unenrolled_eval.py`: both describe/use exhaustive pairwise enrolled-vs-non-enrolled score distributions.
- `scripts/unenrolled_eval.py` now avoids the old full-subject impostor duplication, but `paper.tex` score-count table and some score artifacts remain stale or mixed until the corrected run finishes and tables are regenerated.
- Remaining active inconsistencies are concentrated in response-letter stale max-gallery wording, five-fold/five-run claims, train/validation identity-disjoint wording, intra-database protocol wording, processed dataset counts, architecture table values, runtime table values, ablation evidence bases, and LaTeX build dependencies.
- Checkpoint availability is now treated as a release/README checklist item rather than a scientific inconsistency because the user plans to add checkpoint links later.
- The updated `paper.tex` repetition paragraph now uses `stat_seed`/available-run wording, but response-letter sections still contain old five-fold/five-run claims.
- Current active paper issues from the latest pass: FV-300 score-count row still has stale/shifted impostor counts, FV-300/PolyU dataset counts mix raw and processed views, architecture table still mismatches DSConv kernels and Grapher `k`, runtime values are stale, and the loss/beta ablation tables mix evidence bases.
- Current active response-letter issues from the latest pass: stale five-fold wording, subject-disjoint train/validation wording, max-gallery protocol wording, intra `50/30/20` wording, stale runtime values, and overbroad superiority wording at lines 522 and 581.
- Current active supplementary issues: LGFIN significance `n` mismatch, conditional Chen scope ambiguity, stale intra `50/30/20` protocol text, intra table `multirow{6}` with five visible methods, PolyU/VERA included in an intra split-statistics table despite being excluded from intra experiments, subject-disjoint train/validation wording, overbroad shared-seed wording, and FV-300 split-count inconsistencies.

## Next

- After the corrected `unenrolled_eval.py` run finishes, regenerate the score-count/result tables and re-check the full-subject values.
- Patch the response-letter protocol wording and five-fold/five-run claims before final polishing.
- Decide whether dataset tables should report raw dataset specs or processed experimental counts, then make Table 3 and quality-analysis wording consistent.
- Update the FV-300 score-count row, runtime table, architecture table, and ablation wording/evidence before the next full consistency pass.
- Use the current-only `rebuttal/inconsistencies.md` as the cleanup checklist for the next paper/response edits.
- Use the supplementary checklist to align `supplementary_material.tex` after patching paper and response wording.
