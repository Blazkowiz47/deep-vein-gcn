# 2026-07-17 - mobai

## Session

- Device/server: `mobai`; repo: `/home/ubuntu/1Projects/deep-vein-gcn`; branch: `main` at `dd9397d`.
- Reconstructed the exact intra-dataset held-out image manifests for FV-300, FV-USM, and MMCBNU from the implemented sorted-ID 80/20 split and both source image folders.
- Added `scripts/generate_intra_test_csvs.py`; generated ignored CSV artifacts under `ablation/intra_test_csvs/` for seeds 0--4 and verified every path against `scripts/intra_open_set_eval.py`.
- Added `scripts/generate_intra_static_test_csvs.py`; generated ignored seed-0 manifests under `ablation/intra_static_test_csvs/` with raw image plus MCP/RLT/WLD feature paths.
- Held-out counts: FV-300 has 61 IDs and `5491/5491/5496/5499/5490` images for seeds 0--4; FV-USM has 99 IDs and 1188 images for every seed; MMCBNU has 120 IDs and 1200 images for every seed.
- Static MCP/RLT/WLD features exactly cover seed 0 for all three datasets. FV-USM and MMCBNU held-out image lists are identical across all seeds. FV-300 seeds 1--4 each require 22--35 image IDs absent from seed 0, so the present static features cannot support fully matched five-seed FV-300 evaluation without extracting missing features.
- Verification: both generator scripts compile; Ruff reports no issues; all 15 raw CSV manifests exactly equal the evaluator-selected paths.
- Added `scripts/intra_open_static_methods.py` for held-out-only MCP/RLT/WLD evaluation using the repository's Pearson-correlation matcher and the intra evaluator's subject/image selection. Cached deterministic results are in `ablation/intra_open_static_results.json`.
- MCP/RLT produced valid seed-0 intra results. Every copied WLD template is constant zero; following the existing `_corr2` zero-denominator behavior gives all-zero genuine/impostor scores, AUC `0`, EER `100`, and zero TAR on all three datasets.
- Found that `utils/metrics.calculate_eer` returns FAR and FRR arrays in reverse order when collecting worker results. EER and AUC are effectively unchanged, but existing table TAR values use the reversed convention. The static result JSON records both `legacy_table_metrics` and `correct_metrics` explicitly.

## Next

- Extract MCP/RLT/WLD features for the missing FV-300 seed 1--4 held-out images, then score each seed with the same exhaustive pairwise evaluator before adding handcrafted rows to the intra-dataset table.
- Replace/regenerate the all-zero WLD exports before treating WLD intra results as meaningful, and resolve the FAR/FRR reversal before final paper reporting.
