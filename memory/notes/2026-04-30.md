---
date: 2026-04-30
project: gcn-deep-vein
device: raghu2
server: SALT
repo_path: /home/ubuntu/sushrut/gcn-deep-vein
branch: main
commit: 83fe33e311f12be4f33694e13925f8359072d5da
sync_status: draft
tags: [phd, journal-paper, biometrics, gcn, vein-recognition]
---

# 2026-04-30

## Intent

- Initialize durable project memory for ongoing journal-paper work on `raghu2`.
- Capture the current repo state and note where the existing paper artifacts already live.

## Work Done

- Created the `memory/` structure for this repo and linked it to the main knowledge base workstream.
- Recorded `raghu2` on SALT as the active device/server context for this project.
- Captured the current repo state as `main` at `83fe33e311f12be4f33694e13925f8359072d5da` (`2026-04-29`, `chore: updates`).
- Reviewed `rebuttal/paper.tex` against the current evaluation, training, runtime, and significance-testing code paths.
- Added red clarification text to `rebuttal/paper.tex` without deleting the original prose, covering protocol wording, split/repetition wording, updated Chen-vs-proposed result interpretation, and the corrected significance-test claim.
- Updated `plot_det_final.py` so DET/ROC plots display `Chen et al.` consistently when plotting the `chen` runs already present in `final_runs`.
- Updated `plot_det_final.py` again so each method now uses a fixed color and fixed plotting order across all DET/ROC figures, preventing legend/color shifts when some methods are absent from a particular dataset plot.
- Removed `Chen et al.` from the public plotting and final-table generation paths by excluding `chen` from `plot_det_final.py` and deleting it from `scripts/generate_final_tables.py`; regenerated `final_tables.md` accordingly.
- Added `cosface` to `scripts/input_loss_ablation.py` so the loss ablation driver can run CosFace directly through the existing training/evaluation path. The current fallback defaults remain `scale=64.0` and `margin=0.35` when not overridden in the config.
- Added `scripts/graph_k_sensitivity.py` to run the remaining FV-USM graph-construction `k` sensitivity settings for `stat_seed=0`: `(9,9)`, `(18,18)`, and `(9,18)`, while treating `(18,9)` as the existing proposed setting.
- Added `scripts/print_input_loss_ablation_latex.py` to print a LaTeX loss-comparison table with one-sided Wilcoxon tests against the official proposed CAH runs from `run_name_mappings.py` and `ablation/full_subjects_results.json`, restricted to the common seeds `0..3`.
- Current printer output skips CosFace because the cached ablation file is missing at least one of seeds `0..3` for `cosface`.
- Updated `scripts/significance_tests.py` to emit a rebuttal-ready LaTeX table instead of markdown and to add a paired Wilcoxon signed-rank `p`-value column alongside the Holm-adjusted exact sign-flip permutation-test `p`-value.
- Ran a six-agent consistency review over `rebuttal/paper.tex`, `rebuttal/supplementary_material.tex`, and `rebuttal/response_letter.tex` with critic/replier roles per file.
- Created and populated `rebuttal/inconsistencies.md` with 30 confirmed inconsistencies plus one open/risky t-SNE wording issue, grouped by protocol, data/results, architecture/loss, claims, and LaTeX/build risks.

## Experiments / Runs

- Command/config:
- Dataset:
- Output path: `final_runs/`, `ablation/`, `rebuttal/`, `final_tables.md`, `wandb/`
- Result: Existing paper-facing artifacts are present, but their exact generating commands/configs still need to be backfilled into memory.
- Next action: Trace the generating commands/configs from `raghu2` history, scripts, or W&B and record them in `runs.md`.

## Analysis Results

- `final_tables.md` is the current compact summary of the half-subject and full-subject cross-dataset evaluation.
- The proposed method is competitive across several splits, while `ABCD -> E` remains a hard transfer case and `BCDE -> A` at strict low FAR still trails the strongest baseline.
- Main paper/code mismatches found in review:
  - `scripts/unenrolled_eval.py` uses exhaustive pairwise score distributions, not max-over-gallery unknown rejection.
  - leave-one-dataset-out repetitions are driven by predefined `stat_seed` splits, not on-the-fly 5-fold partitioning inside `train.py`.
  - after adding `Chen et al.`, several narrative claims about “best in all splits” and “all p-values < 1e-4” became stale.
- Rebuttal-wide consistency pass found additional confirmed issues:
  - full-subject impostor scores duplicate half-protocol enrolled-vs-unenrolled pairs in `scripts/unenrolled_eval.py`;
  - main cross-dataset artifacts use 4 seeds for most methods, not 5, with FV-USM/LGFIN at 3 seeds;
  - intra-database protocol text claims 50/30/20 unknown rejection but local code uses 80/20 development/left-out pairwise evaluation;
  - FV-300 and PolyU dataset counts mix raw specs and processed experimental counts;
  - CAH loss formula, graph `k`, DSConv kernels, and graph-convolution type in the text differ from local implementation/configs;
  - runtime, significance, Chen inclusion, and loss-ablation claims are inconsistent across TeX and local artifacts;
  - multiple figure paths, bibliography path, and response-letter LaTeX dependencies are missing or stale.
- Re-checked the user's update to `scripts/unenrolled_eval.py`: the duplicate `imposter_scores_full.extend(imposter_scores)` append is now commented out, so future full-subject runs should use only unordered all-subject cross-identity impostor scores. Existing score artifacts/tables are still stale until rerun/regeneration.
- Re-reviewed updated `rebuttal/paper.tex` and `rebuttal/response_letter.tex`, ignoring the CAH-loss formula simplification by user request. The paper's half-subject protocol wording now matches pairwise score construction, but the response letter still has a stale max-gallery sentence; five-fold/five-run wording, stale score-count/runtime tables, dataset-count mismatches, architecture table mismatches, checkpoint claims, intra-protocol wording, and LaTeX dependency/path issues remain active.

## Learnings

- The paper workflow needs a cleaner mapping from final tables back to the exact runs/configs that generated them.

## Decisions

- Use in-repo memory as the operational record and the main knowledge base workstream as the compact cross-project summary.

## Blockers

- Existing result directories are not yet tied to exact commands/configs in memory.
- Rebuttal TeX files currently disagree with local code/artifacts in enough places that prose polishing should wait until protocol/result policy decisions are made.

## Next

- Resolve the high-impact items in `rebuttal/inconsistencies.md`: score duplication, protocol wording, seed counts, intra protocol, dataset counts, and loss/architecture formula mismatches.
- After the current corrected `scripts/unenrolled_eval.py` run finishes, regenerate the score-count and result tables before doing a final line-by-line prose pass.
- Backfill `runs.md` from `raghu2`.
- Link the strongest tables/plots and supporting artifacts into the journal-paper draft workflow.
- After the paper text is stabilized, rerun a final consistency pass so the prose matches the latest tables and significance outputs exactly.
- Regenerate the DET/ROC plots if the rebuttal needs figures with `Chen et al.` included by name in the legend.
